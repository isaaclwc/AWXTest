---
  - hosts: 192.168.0.190
    name: ensure all packages are updated
    become: yes
    tasks:
      - name: ensure all packages are updated
        apt:
          update_cache: true
          state: latest